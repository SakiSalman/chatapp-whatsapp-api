// Function to convert '.' to '-'
export function convertDotToDash(str) {
    return str.replace(/\./g, 'elite');
}

// Function to convert '-' to '.'
export function convertDashToDot(str) {
    return str.replace(/elite/g, '.');
}